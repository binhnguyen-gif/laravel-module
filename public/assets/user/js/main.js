

document.getElementById('btn-down').onclick = function () {
  let contactEl = document.getElementById("content-contact")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-introduce').onclick = function () {
  let contactEl = document.getElementById("content-introduce")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-introduce').onclick = function () {
  let contactEl = document.getElementById("content-introduce")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-program').onclick = function () {
  let contactEl = document.getElementById("content-program")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-student').onclick = function () {
  let contactEl = document.getElementById("content-student")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-career').onclick = function () {
  let contactEl = document.getElementById("content-career")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-activities').onclick = function () {
  let contactEl = document.getElementById("content-activities")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
document.getElementById('btn-down-images').onclick = function () {
  let contactEl = document.getElementById("content-images")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};


document.getElementById('btn-down-industry').onclick = function () {
  let contactEl = document.getElementById("content-industry")
  if (contactEl.offsetHeight == 72 || contactEl.offsetHeight == 48) {
    contactEl.style.height = "auto"
  } else {
    if ($(window).width() > 640) {
      contactEl.style.height = "72px"
    } else {
      contactEl.style.height = "48px"
    }
  }
};
